import { TimerClock } from "./Clock";
import { TimerOptions } from "./Options";
import { TimerSaved } from "./Saved";

export const Timer = {
  Clock: TimerClock,
  Options: TimerOptions,
  Saved: TimerSaved,
};
