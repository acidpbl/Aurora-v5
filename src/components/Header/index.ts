import { HeaderButton } from "./Button";
import { HeaderRoot } from "./Root";
import { HeaderToggle } from "./Toggle";

export const Header = {
  Root: HeaderRoot,
  Button: HeaderButton,
  Toggle: HeaderToggle,
};
